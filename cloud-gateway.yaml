spring:
 cloud:
    gateway:
      server:
        webflux:
          routes:
          - id: payment-route
            uri: lb://payment-service
            predicates:
            - Path = /api/**
        
server:
  port: 9090
  
